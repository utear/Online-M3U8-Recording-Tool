PORT=3001
HOST=0.0.0.0  # 使用 0.0.0.0 允许外部访问
WS_PORT=3002  # WebSocket 服务端口
JWT_SECRET=your-secret-key  # JWT加密密钥，生产环境中应使用强密钥
TOKEN_EXPIRE=24h  # Token过期时间

# CORS 配置，多个域名使用逗号分隔
CORS_ALLOWED_ORIGINS=http://localhost:3005,http://127.0.0.1:3005,http://your-domain.com:3005