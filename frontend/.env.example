# 前端服务器配置
VITE_HOST=0.0.0.0  # 使用0.0.0.0允许外部访问
VITE_PORT=3005  # 前端服务端口

# API和WebSocket地址配置
# 注意：生产环境中应使用实际的域名或IP地址
VITE_API_BASE_URL=http://your-domain.com:3001  # 后端API地址
VITE_WS_URL=ws://your-domain.com:3002  # WebSocket地址

# 允许访问的域名配置
VITE_ALLOWED_HOSTS=localhost,your-domain.com  # 多个域名用逗号分隔